# 보통의하루 — 기능명세서
> 기준일: 2026-05-08 / 코드 전체 직접 확인 기준

---

## 1. 서비스 개요

동작구 보행자 대상 야간 안전 귀갓길 제안 웹 앱.

- **지원 지역**: 서울 동작구 (관악구 코드 잔존, 전부 주석 처리 비활성화)
- **스택**: React + TypeScript / FastAPI / 카카오맵 SDK / TMAP API / Gemini 2.5 Flash
- **지도 초기 중심**: 상도역 (37.5030778, 126.9477711), 줌레벨 4
- **CORS**: `*`, `https://saferoute-two.vercel.app`

---

## 2. API 엔드포인트

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/safety-score` | 지도 클릭 좌표 → 안전점수 반환 |
| GET | `/api/map-points` | 지도 위 전체 마커 데이터 반환 |
| POST | `/api/safe-route` | 출발지·도착지 → 안전경로+일반경로 반환 |
| GET | `/api/search-poi` | 장소 키워드 검색 (TMAP POI, 기본 5개) |
| POST | `/api/route-description` | 경로 구간 → Gemini AI 안전 분석 반환 |

---

## 3. 안전점수 산출

### 3-1. 공식 (기본점수 없음, 순수 가중합)

```
점수 S =
    주간 활동보너스          (주간 +6.0 / 야간 0)
  + CCTV 근거리 점수
  + CCTV 원거리 점수
  + 가로등 점수             (야간만)
  + 보안등 점수             (야간만)
  + 편의점·24시간 점포 점수  (야간/주간 가중치 차등)
  + 경찰서·지구대 점수
  + 소방서 점수
  - 유흥업소 감점           (야간 2.5배)
```

### 3-2. 요소별 반경·방식·최대점수

| 요소 | 실제 사용 반경 | 방식 | 최대 점수 | 시간 조건 |
|---|---|---|---|---|
| CCTV 근거리 | 30m | 고정 (1개=12pt, 2개↑=18pt) | 18점 | 항상 |
| CCTV 원거리 | 30~100m | 체감수익감소 (base 6pt, cap 3개, rate 0.5) | 10.5점 | 항상 |
| 가로등 | 30m | 고정 cap 2개 × 5.5pt | 11점 | **야간만** |
| 보안등 | **18m** | 고정 cap 1개 × 4pt | 4점 | **야간만** |
| 편의점+24시 | 150m | 체감수익감소 (야간 base 3.5 / 주간 base 2.0, rate 0.3) | 야간 11.67 / 주간 6.67점 | 시간차등 |
| 경찰서·지구대 | 200m | 거리감쇠 선형 (0m=14pt → 200m=0pt) | 14점 | 항상 |
| 소방서 | 300m | 거리감쇠 선형 (0m=10.5pt → 300m=0pt) | 10.5점 | 항상 |
| 유흥업소 (감점) | 50m | 고정 cap 2개 × -7.5pt (야간 ×2.5배) | 주간 -15 / 야간 -37.5점 | 시간차등 |
| 주간 활동보너스 | - | 고정 +6.0pt | 6점 | **주간만** |

> **지도 클릭 카운트 표시용 반경** (점수 산출과 무관):
> 가로등 30m / 보안등 10m / 편의점·24시 150m / 경찰서 200m / 소방서 300m / 유흥업소 50m

### 3-3. 야간/주간 기준 (KST)

- **야간**: 20시 이상 또는 07시 미만 (`h >= 20 or h < 7`)
- **주간**: 07시 이상 ~ 20시 미만

### 3-4. 등급 기준 (가중합 점수 S)

| 등급 | 조건 |
|---|---|
| 위험 | S ≤ 0 |
| 보통 | 0 < S < 16 |
| 안전 | S ≥ 16 |

---

## 4. 안전경로 탐색 알고리즘

### 전체 흐름

1. TMAP 도보경로 API로 일반경로 조회
2. **50m 간격** 샘플링 → 첫 비안전(보통/위험) 구간 인덱스 탐지
3. 안전 구간 좌표 누적 후 해당 지점에서 **전방 180° 7방향 레이더 스캔**
4. 경유지 → 목적지 TMAP 재호출, **무한 반복** (횟수 제한 없음)
5. 좌표 이어붙이기 후 **역주행 구간 제거** (`proximity_m=10.0m`)
6. 안전경로 소요시간 > 일반경로 × **1.3배** → 일반경로로 대체

### 레이더 스캔 상세 (`_best_ray_waypoint`)

- **각도 7방향**: -90°, -60°, -30°, 0°, +30°, +60°, +90°
- **레이더 길이**: `연속 비안전 구간 수 × 50m`
- **샘플 간격**: 30m
- **피크 탐지**: 점수 증가→감소 전환점 (피크)
- **후보 조건**: 피크 점수가 안전 등급 (S ≥ 16)인 것만
- **최종 경유지**: 후보 중 목적지에 가장 가까운 지점

---

## 5. Gemini AI 경로 분석

- **모델**: `gemini-2.5-flash`

### AI 분석 시 사용하는 요소별 반경

| 요소 | 반경 |
|---|---|
| CCTV | 30m |
| 가로등 | 30m |
| 보안등 | 10m |
| 편의점·24시간 점포 | 150m |
| 경찰서·지구대 | 200m |
| 소방서 | 300m |
| 유흥업소 | 50m |

### 출력 구조

```json
{
  "safe_points": ["최대 3개"],
  "warning_points": ["최대 3개"],
  "ai_summary": "2문장 이내 행동 조언"
}
```

### 언급 우선순위 (점수 기여도 순)

1순위 경찰서·지구대 (14pt) → 2순위 소방서 (10.5pt) → 3순위 편의점·24시 (7.5pt) → 4순위 CCTV근거리 (12pt) → 5순위 CCTV원거리 (6pt) → 6순위 가로등 (5.5pt) → 7순위 보안등 (4pt) / 감점 유흥업소 (-7.5pt)

### 주요 서술 규칙

- 소방서: "야간 대피처·신고처" 관점만, 화재·재난 언급 금지
- 경찰서 있으면 소방서 부재 경고 생략
- 조명: 가로등·보안등 중 수치 많은 쪽 위주, 둘 다 부족할 때만 경고
- 밀도 단위: "50m당" 사용, "구간당" 표현 금지

---

## 6. 지도 UI

### 마커 레이어 토글 (8종류)

| 레이어 | 대표 색상 |
|---|---|
| CCTV | 파랑 `#3b82f6` |
| 가로등 | 주황-노랑 `#fbbf24` |
| 보안등 | 연두 `#bef264` |
| 편의점 | 보라 `#d8b4fe` |
| 24시간 점포 | 검정 `#3c3c3c` |
| 경찰서·지구대 | 청록 `#67e8f9` |
| 소방서 | 주황 `#f97316` |
| 유흥업소 | 빨강 `#ef4444` |

클러스터 경계값: 12 / 45 / 110 / 360개 / 원 크기: 32→40→48→56→64px

### 지도 클릭 안전도 패널 (PC 전용)

- 등급 배지 (안전/보통/위험, 색상 구분)
- 요소별 칩 8개: CCTV / 가로등 / 보안등 / 편의점 / 24시간 점포 / 경찰서·지구대 / 소방서 / 유흥업소
  - 있으면 초록 테두리, 없으면 회색, 유흥업소는 빨강
- 숫자 점수 미표시
- 좌표 표시 (소수점 5자리)

---

## 7. 경로 카드 UI

- 안전 경로 / 일반 경로 카드 각 1개
- 표시 항목: 경로 제목 / 등급(안전·보통·위험, 18px) / 소요시간(분) / 거리(1km↑=N.Nkm, 미만=Nm)
- 숫자 점수 미표시
- 폴리라인: 등급 색상 단일선 (안전=초록 / 보통=노랑 / 위험=빨강 / 일반=파랑 `#2563eb`)

---

## 8. 장소 검색 자동완성

- 입력창 1개 (장소명·주소·건물명 통합)
- **debounce 50ms** 후 `/api/search-poi` 호출 (TMAP POI)
- 드롭다운 최대 **5개**, 선택 시 좌표 자동 설정
- 2글자 미만 입력 시 suggestions 초기화

---

## 9. 현재위치 추적

- 단일 버튼 "현재위치 추적" (PC 사이드바)
- `watchPosition` 기반 실시간 추적, 첫 fix에서 출발지 자동 설정
- 추적 중 초록색 버튼으로 상태 표시

---

## 10. 데이터 현황

### 활성화 (동작구)

| 파일 | 내용 |
|---|---|
| `cctv_dongjak.csv` | CCTV (qty 포함) |
| `streetlight_dongjak.csv` | 가로등 |
| `safelight_dongjak.csv` | 보안등 |
| `convenience_dongjak.csv` | 편의점 |
| `open24_dongjak.csv` | 24시간 점포 |
| `police_dongjak.csv` | 경찰서·지구대·파출소 |
| `firestation_dongjak.csv` | 소방서 |
| `entertainment_dongjak.csv` | 유흥업소 |

### 비활성화 (관악구, 전부 주석 처리)

`cctv_raw.csv` / `streetlight_raw.csv` / `convenience_gwanak.csv` / `police_gwanak.csv` / `entertainment_gwanak.csv` / `safety_grid.csv`

---

## 11. 알려진 문제 / 미결 과제

| 우선순위 | 내용 |
|---|---|
| 🔴 | 동작구 대부분이 보통 등급 — 안전/위험 구분 희박 |
| 🟡 | 안전 등급(S≥16) 지역 부족 → 레이더 경유지 못 찾고 일반경로 반환 빈번 |
| 🟡 | 역주행 제거(10m 임계값)가 정상 경로 일부 제거 가능성 |
| 🟢 | 모바일 지도 클릭 안전도 확인 불가 |
| 🟢 | 서울 전역 확장 미완 (동작구만) |
