# 프로젝트 규칙

## 언어
- **반드시** 한국어로만 응답할 것
- 영어로 질문받아도 한국어로 답변
- 코드 주석도 한국어로 작성
- 어떤 상황에서도 영어 응답 금지

## 작업 시작 전
- 모든 작업 요청을 받으면 바로 실행하지 말고
  "Plan Mode로 먼저 계획을 확인할까요?" 라고 물어볼 것
- 사용자가 승인하면 계획을 보여주고, 승인 후 실행

## 작업 완료 후
- 작업이 끝나면 항상 마지막에 /cost 명령어를 실행해서
  현재 세션의 토큰 사용량과 비용을 알려줄 것

  ## 코딩 규칙
- 파일 수정 전 반드시 현재 파일 내용 먼저 확인
- 기존 코드 스타일 유지 (임의로 리팩토링 금지)
- 한 번에 여러 파일 대규모 수정 금지 → 하나씩 확인 후 진행

## 오류 처리
- 에러 발생 시 원인 한국어로 설명 후 수정
- 확실하지 않은 건 추측으로 수정하지 말고 먼저 물어볼 것

## 커뮤니케이션
- 작업 시작 전 "무엇을 할 것인지" 한 줄 요약
- 작업 중 진행상황 단계별로 알려주기
- 모르거나 애매하면 추측하지 말고 질문할 것

## 비전공자 배려
- 코드 작성 후 "이 코드가 하는 일"을 한국어로 2~3줄 요약
- 생소한 개념이 나오면 쉬운 말로 부연 설명
- 왜 이 방법을 선택했는지 간단히 이유 설명

## 플러그인 사용 규칙

### Context7
- 라이브러리, 프레임워크 관련 코드 작성 시 항상 context7 사용
- 프롬프트에 "use context7"을 자동으로 포함해서 실행
- Next.js, React, Tailwind, Supabase 등 외부 라이브러리는
  반드시 context7으로 최신 문서 확인 후 코드 작성

### Feature-dev
- 새로운 기능 개발 요청이 오면 바로 코드 짜지 말고
  /feature-dev 워크플로우로 먼저 설계 단계 진행
- "~만들어줘", "~추가해줘", "~구현해줘" 같은 요청은
  feature-dev 대상으로 판단

## 터미널 출력 처리
- bash 명령 실행 후 출력 결과를 항상 한국어로 해석해서 설명
- 에러 메시지가 나오면 원문 그대로 두고 아래에 한국어 설명 추가
- 성공/실패 여부를 한국어로 요약

---

# 인수인계 (2025-05-03 기준)

## 서버 실행 방법
```
# 백엔드 (포트 8000)
cd backend
venv\Scripts\uvicorn.exe main:app --reload

# 프론트엔드 (포트 3000)
cd frontend
npm start
```

## 환경변수 (.env 파일 — git에 없음, 따로 받아야 함)
| 파일 위치 | 변수명 | 용도 |
|---|---|---|
| `backend/.env` | `TMAP_APP_KEY` | 경로 탐색 API |
| `backend/.env` | `GEMINI_API_KEY` | AI 경로 설명 |
| `frontend/.env` | `REACT_APP_KAKAO_MAP_KEY` | 카카오맵 표시 |

## 현재 구조 요약
- **프론트**: React + TypeScript + 카카오맵 SDK
- **백엔드**: FastAPI (Python), `/api/safe-route`, `/api/safety-score`, `/api/map-points`, `/api/search-poi`, `/api/route-description`
- **데이터**: 동작구 전용 (관악구 데이터는 코드에 남아있지만 전부 비활성화됨)

## 안전점수 산출 방식 (현재)

격자 CSV 방식을 완전히 버리고 **실시간 반경 KDTree 방식**으로 전환됨.
지도 클릭 점수(`safety.py`)와 경로 점수(`route.py`) 모두 동일 로직 사용.

### 점수 공식
```
점수 = 40(기본) + CCTV근거리 + CCTV원거리 + 가로등 + 보안등 + 편의점 + 경찰서 - 유흥주점감점
최솟값 = 10점 강제
```

### 요소별 설정 (고정캡 × 단일점수 방식 — 체감수익감소 아님)
| 요소 | 반경 | 최대 개수 | 1개당 점수 | 최대 기여 |
|---|---|---|---|---|
| CCTV 근거리 | 30m | 2개 | 6.0점 | 12점 |
| CCTV 원거리 | 70m | 2개 | 3.5점 | 7점 |
| 가로등 | 20m | 2개 | 5.5점 | 11점 |
| 보안등 | 5m | 1개 | 4.0점 | 4점 |
| 편의점 | 100m | 2개 | 3.5점 | 7점 |
| 경찰서/지구대 | 200m | 1개 | 14.0점 | 14점 |
| 유흥주점 (감점) | 50m | 2개 | -7.5점 | -15점 |
| **자연 최대 합계** | | | | **95점** |

### 등급 기준 (절대값)
- 위험: 40점 미만
- 보통: 40~60점
- 안전: 60점 이상

## 안전경로 탐색 로직

1. TMAP으로 일반 경로 먼저 조회
2. 50m 간격으로 샘플링 → 첫 번째 비안전(보통/위험) 구간 탐지
3. 해당 지점에서 **전방 180° 7방향 레이더 스캔** (각도: -90°~+90°, 30° 간격)
   - 레이더 길이: `max(200m, 연속위험개수 × 50m)`
   - 각 방향에서 30m 간격 샘플링 → 점수 증가→감소 피크 탐지
   - 피크가 "안전(60점↑)" 등급인 것만 후보
   - 후보 중 목적지에 가장 가까운 지점 = 경유지
4. 경유지 → 목적지로 TMAP 재호출, 반복 (최대 8회)
5. 전체 경로 이어붙이기 후 루프(되돌아가기) 제거

## 현재 알려진 문제점 / 미결 과제

### 🔴 점수 차등 문제 (가장 중요)
- 동작구 전체 격자 분석 결과: **94.7%가 보통(40~60점)에 몰림**
- 안전 4.2%, 위험 1.1%로 등급 구분이 사실상 무의미한 수준
- 원인: 기본점수 40점이 보통 하한선과 같아서 요소 없어도 자동 보통
- 해결 방향 두 가지로 논의 중이었음:
  - **A안**: 기본점수 낮추기 (20~25점) → 요소 없으면 위험으로 내려감
  - **B안**: 임계값을 실제 분포에 맞게 재조정 (예: 보통 40~55, 안전 55↑)
- **결정 안 남, 친구가 이어서 논의/결정해야 함**

### 🟡 안전경로 레이더가 안전지점을 못 찾는 경우
- 동작구 안전(≥60점) 지역이 4.2%뿐이라 레이더가 경유지를 못 찾고 일반경로 그대로 반환하는 경우 빈번
- 점수 차등 문제 해결되면 자연히 개선됨

### 🟡 _remove_backtrack 공격성
- `route.py`의 `_remove_backtrack` 함수 (proximity_m=20)가 정상 경로도 일부 제거할 수 있음
- 문제 재현되면 threshold 낮추거나 제거 조건 강화 필요

### 🟢 동작구만 지원
- 관악구 데이터/코드는 주석 처리된 채로 남아있음
- 향후 서울 전역 확장 시 각 `_load_*()` 함수에 구 추가하면 됨

## 주요 파일 위치
| 파일 | 역할 |
|---|---|
| `backend/routers/safety.py` | 안전점수 계산 (KDTree, 지도클릭 API) |
| `backend/routers/route.py` | 경로탐색 로직 (안전경로 알고리즘) |
| `backend/routers/ai_description.py` | Gemini AI 경로 설명 생성 |
| `backend/data/analyze_distribution.py` | 동작구 점수분포 분석 스크립트 (개발용) |
| `frontend/src/App.tsx` | 프론트엔드 전체 (지도, UI, API 연동) |
| `frontend/src/App.module.css` | 스타일 |
